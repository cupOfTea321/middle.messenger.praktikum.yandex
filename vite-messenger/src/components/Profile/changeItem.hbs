
<div class="profile-data__row {{class}}" id="profile-data">
    <label class="bold" for="{{name}}">{{first}}</label>
    <input   id="{{name}}" name="{{name}}" class="second"  value="{{second}}" />
    {{#if req}}
        <span class="error_message">
            {{error}}
        </span>
    {{/if}}
</div>
